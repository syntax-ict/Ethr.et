{{-- The closing call to action: the staff sign-in, on this tenant's own host. --}}
<div class="cta">
    <a class="btn btn--primary btn--lg" href="/login">{{ __('public.employee_sign_in') }}</a>
</div>
